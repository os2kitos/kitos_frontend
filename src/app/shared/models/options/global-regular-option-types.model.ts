/**
 * Naming convention {module-context}_{option-type-name}
 * Used for option types that should only be accessible to global admins
 */
export type GlobalRegularOptionType = 'organization_country-code'
