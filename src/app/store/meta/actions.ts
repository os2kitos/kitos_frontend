import { createAction } from '@ngrx/store';

export const resetStateAction = createAction('[Meta] Reset state');
export const resetOrganizationStateAction = createAction('[Meta] Reset organization state');
